'use client'

// The app already has a polished sidebar at `components/layout/sidebar`.
// This wrapper exists so the Resume dashboard can keep its component-driven structure.
export { Sidebar } from '@/components/layout/sidebar'

